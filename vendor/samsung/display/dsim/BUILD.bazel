# Samsung DSIM (Display Serial Interface Module)

load("//common:kernel.bzl", "ddk_module")

ddk_module(
    name = "samsung_dsim",
    srcs = glob(["**/*.c"]),
    out = "samsung_dsim.ko",
    kernel_build = "//common:kernel_aarch64",
    deps = ["//common:all_headers_aarch64"],
    visibility = ["//visibility:public"],
)
