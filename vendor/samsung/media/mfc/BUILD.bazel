# Samsung MFC (Multi-Function Codec) — Exynos 1280

load("//common:kernel.bzl", "ddk_module")

ddk_module(
    name = "samsung_mfc",
    srcs = glob(["**/*.c"]),
    out = "samsung_mfc.ko",
    kernel_build = "//common:kernel_aarch64",
    deps = ["//common:all_headers_aarch64"],
    visibility = ["//visibility:public"],
)
