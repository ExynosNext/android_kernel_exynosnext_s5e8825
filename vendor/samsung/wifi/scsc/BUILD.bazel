# Samsung SCSC WiFi/BT — Exynos 1280

load("//common:kernel.bzl", "ddk_module")

ddk_module(
    name = "samsung_wifi",
    srcs = glob(["**/*.c"]),
    out = "wlan.ko",
    kernel_build = "//common:kernel_aarch64",
    deps = ["//common:all_headers_aarch64"],
    visibility = ["//visibility:public"],
)
